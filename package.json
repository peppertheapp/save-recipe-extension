{
  "name": "pepper-extension",
  "version": "1.0.0",
  "description": "Pepper Recipe Importer — save any recipe to your Pepper profile in one click",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src",
    "test": "vitest run",
    "test:watch": "vitest",
    "gen:icons": "node scripts/gen-icons.mjs",
    "mock-api": "node scripts/mock-api.mjs",
    "zip": "npm run build && cd dist && rm -f ../pepper-extension.zip && zip -qr ../pepper-extension.zip . -x '*.map' && cd .. && ls -la pepper-extension.zip"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.0.0",
    "@types/chrome": "^0.0.280",
    "eslint": "^9.15.0",
    "jsdom": "^25.0.1",
    "prettier": "^3.3.3",
    "typescript": "^5.6.3",
    "typescript-eslint": "^8.15.0",
    "vite": "^6.0.1",
    "vitest": "^3.2.7"
  }
}
